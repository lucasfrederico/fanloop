apiVersion: v2
name: fanloop
description: Real-time fan-out gateway with a Redis backplane
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: redis
    version: "25.5.3"
    repository: "https://charts.bitnami.com/bitnami"
    condition: redis.enabled
